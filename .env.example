# URL completa de la API tp_parte3 (incluye el subpath /tp_parte3/api porque
# así queda servida dentro del contenedor Apache). Cambiala si el backend
# corre en otro host/puerto.
VITE_API_BASE_URL=http://localhost:8081/tp_parte3/api
